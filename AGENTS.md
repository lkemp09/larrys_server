# Codex Instructions

Use the organized instructions in the `docs/` directory as the source of truth for project-specific guidance.

Durable Docs is a documentation concept, not a filename. Store durable instructions and notes in topic-specific files under `docs/`.

Read relevant files in `docs/` before planning or changing related parts of the repository. Start with:

- `docs/railway.md` for Railway deployment and connection details.
- `docs/postgres.md` for database and PostgreSQL conventions.

## Modification Rule

Files may be read freely.

No files may be created, modified, moved, renamed, deleted, staged, committed, or otherwise changed unless the user has explicitly given one of these exact approvals in the current request: 

- `go`
- `Go`
- `GO`
 